# Default local/Vercel setup uses the same-origin /backend proxy.
VITE_API_URL=/backend

# Keep false unless you intentionally want Vercel serverless auth endpoints.
VITE_USE_SERVERLESS_AUTH_API=false
